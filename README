Triangle as a Vulkan HPP practice
(Deprecated since vk::DispatchLoaderDynamic behaviour changed)

Dependencies are:
 -Vulkan compatible GPU
 -Clang with C++17 support
 -GLFW for window
 -GLM for math
 -SPIR-V compiler

To compile and run:
 make
 ./triangle
